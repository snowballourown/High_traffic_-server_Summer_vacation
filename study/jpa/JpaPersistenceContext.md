## 영속성 컨텍스트 

- 엔티티 메니저 
엔티티를 관리하는 거라고 생각    
데이터 트랜젝션단위로 만들고 단위가 끝날떄 종료 시켜버림     
말이 캐쉬를 관리하는거지  큰단위가 아니라 성능상에서 큰이점을 가져갈건 없음   
- 비즈니스 로직이 큰 경우는 도움이 됨 



em.persist(member);   
->

1차 캐시에 저장됨

@Id 로 설정한 Pk가 
캐시에서 id 역할을함

그리고 @Entity가 1차캐쉬에 객채로 들어감 
 

em.find(member.class, "member1")를 시도하면

1차캐쉬에 있는지확인하고 없으면 
-> DB에 접근함 
-> 가져온걸 1차 캐쉬에 저장하고 
-> DB에 저장함 

% DB에 저장할때 쿼리가나감 


## dirty checking(변경 감지)
1차 context에 들어온 녀석을 변경시 자동으로 감지해 
update 쿼리가 나가도록함 
- 1차 context에 들어온 시점에 스냅샷(현재 정보를 적어둠)
- 그리고 commit되는 시점에 비교하여 값이 변경되면 update 쿼리를 날려줌


- flush 발생 3가지 
  - 변경감지
  - 수정된 엔티티 쓰기 지연 SQL저장소에 등록
  - 쓰기지연 SQL저장소의쿼리를 데이테베이스에 전송(등록, 수정, 삭제쿼리)


if)   
em.persist(m1);   
em.persist(m2);   
em.persist(m3);   

em.creatQueary(Member.class, select m from Member m);  
// 이렇게 JPQL를 쓰면 DB에 반영이 안될것까지만   
// JPA에서는 이걸 방지하기위해서 JPQL 쿼리가나가면 자동으로 flush를 하게 만들어 놓음 


#### flush
- 영속성 컨텍스트를 비우지 않음(그니까 초기화시키는게 아니다)
- 영속성 컨텍스트의 변경내용을 데이터베이스에 동기화
- 트랜젝션이라는 작업단위가 중요 -> 커밋 직전에만 동기화 하면됨